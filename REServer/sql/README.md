# Legacy SQL schema (Postgres / Supabase)

These scripts describe the **original relational schema**. The server now uses **Firestore**; see [../firestore/DATA_MODEL.md](../firestore/DATA_MODEL.md) for the document model and loading guidance.

Use the SQL files as a reference when migrating existing data into Firestore collections.
